version = '1.4.0'
//version += '-SNAPSHOT'

dependencies {
}

jar {
    manifest {
        instructionFirst 'Import-Package', "!org.immutables.value"
        instruction 'Import-Package', "*"
    }
}