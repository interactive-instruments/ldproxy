const foundationFeature = "foundation";
const apiBuildingBlocks = (name) => `apiBuildingBlocks.${name || "*"}`;
const collectionEditTabs = (name) => `collectionEditTabs.${name || "*"}`;

export { foundationFeature, apiBuildingBlocks, collectionEditTabs };
