{{> header}}

<div>
    <h1>{{title}}</h1>
    <p>{{{description}}}</p>
    <br/>
    <br/>
    <br/>

    <ul class="list-unstyled space-after">
        {{#data}}
        {{#uri}}
        <li>
            <h2>
                <a href="{{path}}{{id}}">
                    <span>{{label}}</span>
                </a>
            </h2>
            <div>{{{description}}}</div>
        </li>
        {{/uri}}
        {{/data}}
        {{^data}}
        <li>None</li>
        {{/data}}
    </ul>

</div>

{{> footer}}
