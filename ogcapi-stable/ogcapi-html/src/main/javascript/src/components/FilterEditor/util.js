export const extractFields = (queryables) => {
  console.log(queryables);

  return { fistname: "Vorname" };
};

export const extractInterval = (collection) => {
  console.log(collection);

  return { start: 0, end: 0 };
};

// TODO: other extractions
