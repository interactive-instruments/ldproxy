version = '1.4.0'
//version += '-SNAPSHOT'

dependencies {
    compile project(":ldproxy-wfs3")
    compile project(":ldproxy-wfs3-api")
    compile project(":ldproxy-wfs3-core")

    testCompile 'org.codehaus.groovy.modules.http-builder:http-builder:0.7'

    //provided group: 'de.interactive_instruments', name: 'xtraplatform-kvstore-file', version: '1.1.2' api statt file

}
//
jar {
    manifest {
        instructionFirst 'Import-Package', "!org.immutables.value"
        instruction 'Import-Package', "*"
    }
}