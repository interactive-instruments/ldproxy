{{> header}}

<div>
    <h1>{{title}}</h1>
    <span>{{{description}}}</span>
    <br/>
    <br/>

    <div class="row my-3">
        <div class="col-md-2 font-weight-bold">{{title}}</div>
        <div class="col-md-10">
            <ul class="list-unstyled">
                {{#ProcessedStyles}}
                    <li>
                        {{#title}}{{.}} ({{id}}){{/title}}{{^title}}{{id}}{{/title}}
                        <ul>
                            {{#linksSorted}}
                                <li class="list-unstyled"><a href="{{href}}" target="_blank">{{title}}</a></li>
                            {{/linksSorted}}
                        </ul>
                        <br/>
                    </li>
                {{/ProcessedStyles}}
                {{^ProcessedStyles}}
                    <li>{{none}}</li>
                {{/ProcessedStyles}}
            </ul>
        </div>
    </div>
</div>

{{> footer}}

