ext {
    swaggerUiVersion = '2.1.3'
}

dependencies {
    // TODO: get as jar
}

jar {
    manifest { 
        instruction 'X-Web-Resource-Version', '1.1'
        instruction 'X-Web-Resource', '/swagger-ui;/swagger-ui'
        instruction 'X-Web-Resource-Default-Page', 'index.html'
    }
}
